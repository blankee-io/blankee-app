-- High Priority Indexes (Execute these first)
CREATE INDEX idx_income_entries_category_date ON income_entries(category_id, date);
CREATE INDEX idx_expense_entries_category_date ON expense_entries(category_id, date);
CREATE INDEX idx_income_entries_date ON income_entries(date);
CREATE INDEX idx_expense_entries_date ON expense_entries(date);
CREATE INDEX idx_totals_remainders_user_date ON totals_remainders(user_id, date);
CREATE INDEX idx_c_expense_entries_category_date ON c_expense_entries(category_id, date);

-- Medium Priority Indexes
CREATE INDEX idx_income_categories_user_display ON income_categories(user_id, display_order);
CREATE INDEX idx_expense_categories_user_display ON expense_categories(user_id, display_order);
CREATE INDEX idx_c_payment_entries_account_date ON c_payment_entries(account_id, date);

-- Lower Priority But Still Beneficial
CREATE INDEX idx_recurring_income_user_category ON recurring_income(user_id, category_id);
CREATE INDEX idx_recurring_expense_user_category ON recurring_expense(user_id, category_id);
CREATE INDEX idx_users_username ON users(username);

----------------------------------------
PRIOR TO INDEX IMPLEMENTATION:
-- One of the most expensive queries: Aggregating income entries by date for daily totals
-- This query is called in update_daily_totals() and runs for every date in the range
Key metrics to note:
Rows examined: Likely high (full table scan on income_entries)
Execution time: Baseline performance
Cost: MySQL's estimated cost
----------------------------------------
EXPLAIN ANALYZE
SELECT ie.date, SUM(ie.amount)
FROM income_entries ie
JOIN income_categories ic ON ie.category_id = ic.id
WHERE ic.user_id = 1 AND ie.date >= '2024-01-01'
GROUP BY ie.date
ORDER BY ie.date;
+-------------------------------------------------------------------------------------------------------------------------------------------------------
| EXPLAIN                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
+-------------------------------------------------------------------------------------------------------------------------------------------------------
| -> Sort: ie.`date`  (actual time=0.0597..0.0597 rows=0 loops=1)
    -> Table scan on <temporary>  (actual time=0.0555..0.0555 rows=0 loops=1)
        -> Aggregate using temporary table  (actual time=0.054..0.054 rows=0 loops=1)
            -> Nested loop inner join  (cost=0.787 rows=0.417) (actual time=0.0128..0.0128 rows=0 loops=1)
                -> Covering index lookup on ic using income_categories_ibfk_1 (user_id=1)  (cost=0.35 rows=1) (actual time=0.0113..0.0113 rows=0 loops=1)
                -> Filter: (ie.`date` >= DATE'2024-01-01')  (cost=0.354 rows=0.417) (never executed)
                    -> Index lookup on ie using income_entries_ibfk_1 (category_id=ic.id)  (cost=0.354 rows=1.25) (never executed)
 |
+-------------------------------------------------------------------------------------------------------------------------------------------------------
1 row in set (0.01 sec)

POST INDEX IMPLEMENTATION:
----------------------------------------
EXPLAIN ANALYZE
SELECT ie.date, SUM(ie.amount)
FROM income_entries ie
JOIN income_categories ic ON ie.category_id = ic.id
WHERE ic.user_id = 1 AND ie.date >= '2024-01-01'
GROUP BY ie.date
ORDER BY ie.date;
+-------------------------------------------------------------------------------------------------------------------------------------------------------
| EXPLAIN                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
+-------------------------------------------------------------------------------------------------------------------------------------------------------
| -> Sort: ie.`date`  (actual time=0.0148..0.0148 rows=0 loops=1)
    -> Table scan on <temporary>  (actual time=0.0113..0.0113 rows=0 loops=1)
        -> Aggregate using temporary table  (actual time=0.0105..0.0105 rows=0 loops=1)
            -> Nested loop inner join  (cost=4.62 rows=35.2) (actual time=0.00568..0.00568 rows=0 loops=1)
                -> Covering index lookup on ic using income_categories_ibfk_1 (user_id=1)  (cost=0.35 rows=1) (actual time=0.00497..0.00497 rows=0 loops=1)
                -> Filter: (ie.`date` >= DATE'2024-01-01')  (cost=4.27 rows=35.2) (never executed)
                    -> Index lookup on ie using income_entries_ibfk_1 (category_id=ic.id)  (cost=4.27 rows=35.2) (never executed)
 |
+-------------------------------------------------------------------------------------------------------------------------------------------------------
1 row in set (0.00 sec)
----------------------------------------

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 
----------------------------------------
High-Impact Indexes (Priority 1)
----------------------------------------

1. income_entries - Composite index for date range queries
CREATE INDEX idx_income_entries_category_date ON income_entries(category_id, date);
Why: frequent queries in app.py joining income_entries with income_categories 
and filtering by user_id and date range. This composite index will dramatically 
speed up the JOIN and date filtering.

2. expense_entries - Composite index for date range queries
CREATE INDEX idx_expense_entries_category_date ON expense_entries(category_id, date);
Why: similar pattern for expenses. This is one of the most frequently 
executed queries in the dashboard updates.

3. income_entries - Index on date alone for aggregations
CREATE INDEX idx_income_entries_date ON income_entries(date);
Why: Used in multiple GROUP BY date queries for calculating daily totals.

4. expense_entries - Index on date alone for aggregations
CREATE INDEX idx_expense_entries_date ON expense_entries(date);
Why: Same as above for expense aggregations.

5. totals_remainders - Covering index for date lookups
CREATE INDEX idx_totals_remainders_user_date ON totals_remainders(user_id, date);
Why: Already has a UNIQUE key, but creating this index explicitly 
will optimize the frequent date range queries.

6. c_expense_entries - Composite index for credit account expenses
CREATE INDEX idx_c_expense_entries_category_date ON c_expense_entries(category_id, date);
Why: Similar reasoning as above; frequent date-based queries for credit account expenses.

----------------------------------------
Medium-Impact Indexes (Priority 2)
----------------------------------------

1. income_categories - Index for display order queries
CREATE INDEX idx_income_categories_user_display ON income_categories(user_id, display_order);
Why: Frequently used to fetch and order categories for dashboard display.

2. expense_categories - Index for display order queries
CREATE INDEX idx_expense_categories_user_display ON expense_categories(user_id, display_order);
Why: Same pattern as income categories.

3. c_payment_entries - Composite index for account/date queries
CREATE INDEX idx_c_payment_entries_account_date ON c_payment_entries(account_id, date);
Why: Used in credit account balance calculations.

----------------------------------------
Additional Optimization Indexes (Priority 3)
----------------------------------------

1. recurring_income - Composite index for user/category lookups
CREATE INDEX idx_recurring_income_user_category ON recurring_income(user_id, category_id);
Why: Used when managing recurring income entries.

2. recurring_expense - Composite index for user/category lookups
CREATE INDEX idx_recurring_expense_user_category ON recurring_expense(user_id, category_id);
Why: Same pattern for recurring expenses.

3. users - Index on username for login
CREATE INDEX idx_users_username ON users(username);
Why: Login queries filter by username (though less frequent, it's critical for user experience).

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

Expected Performance Improvements
Dashboard loads: 40-60% faster (especially update_daily_totals, update_weekly_totals)
Entry additions: 20-30% faster
Date range queries: 50-70% faster
Category management: 30-40% faster

Important Notes:

Existing indexes: Some tables already have foreign key indexes, 
but composite indexes will provide better optimization for multi-column queries

Monitoring: After adding these indexes, monitor query performance using EXPLAIN to verify they're being used

Maintenance: Run OPTIMIZE TABLE after creating indexes on large tables

Trade-offs: Indexes will slightly slow INSERT/UPDATE operations 
but will dramatically improve SELECT performance, which is the predominant operation in this app

These indexes target the most expensive queries identified in 
update_daily_totals, update_weekly_totals, and update_monthly_totals functions, 
which are the performance bottlenecks in the application.